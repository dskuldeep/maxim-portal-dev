# Hidden Page

This page is hidden from the sidebar but still accessible via direct URL.

In `_meta.ts`:
```typescript
export default {
  'hidden-page': {
    title: 'Hidden Page',
    display: 'hidden',
  },
}
```

